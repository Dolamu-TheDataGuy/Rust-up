// installing rust on Linux

// curl http://sh.rustup.rs -sSf | sh

// step 3 - run source $HOME

// libraries are called crate in Rust

//

//Setting up a new project --> cargo new <project_name> --bin


// Compiling --> cargo build

// run the program --> cargo run

//Chapter 3

//All data types in rust are immutable by default, the keyword mut makes the data type mutable.

// Easy concurrency and Safety, you have the flexibility of making variables mutable.

// declare constants with the `mut` keyword. In rust you must always annotate types.

const MAX_POINTS: u32 = 100_000;

